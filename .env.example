# OMDb API Key (Get one for free at https://www.omdbapi.com/apikey.aspx)
OMDB_API_KEY=fbd55d5e

# Session Secret (Strong, random string)
SESSION_SECRET=your-super-secret-key

# Database Connection
DB_HOST=db
DB_PORT=5432
DB_USER=user
DB_PASSWORD=password
DB_NAME=seriestracker
DB_SSLMODE=disable

# SMTP Configuration (For MailHog in dev, or real SMTP in production)
SMTP_HOST=mailhog
SMTP_PORT=1025

# External Port for the Web Interface
APP_PORT=8081
